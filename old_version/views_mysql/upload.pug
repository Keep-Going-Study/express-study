doctype html
html
    head
        meta(charset='utf-8')
    body
        //- 
            <form>의 enctype 속성은 폼 데이터가 
            서버로 보내질 때 인코딩 되는 방식을 명시한다.
        form(action='upload' method='post' enctype="multipart/form-data")
                input(type='file' name='userfile')
                input(type='submit')