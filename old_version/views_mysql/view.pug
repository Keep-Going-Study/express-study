doctype html
html
    head
        meta(charset='utf-8')
    body
        include header.pug
        
        ul
            each _topic in topics
                li
                    a(href=`/topic/${_topic.id}`)=_topic.title

        article
            //- topic : 해당 토픽의 상세 데이터
            
            if topic 
                h2=topic.title
                =topic.description
                br
                br
                div=`by ${topic.author}`
            else
                h2 Welcome
                | This is topic homepage
        ul
            li
                a(href='/topic/add') add
                
            //- 상세보기 페이지에서만(topic 인자 존재) edit,delete 링크 표시
            
            if topic 
                li
                    a(href=`/topic/${topic.id}/edit`) edit
                li 
                    a(href=`/topic/${topic.id}/delete`) delete